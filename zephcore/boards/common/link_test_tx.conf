# Link test TX role override (see link_test.conf for the common settings)
#
# Role is set via a .conf file (not -D) because Kconfig string values need
# the quotes preserved — west/cmake strip them from -D arguments and the
# assignment then aborts the build ("malformed string literal").
CONFIG_ZEPHCORE_LINK_TEST_ROLE="tx"
